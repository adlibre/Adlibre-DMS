# Example custom settings for Adlibre DMS

# Turn off demo
DEMO = False
DEBUG = False
TEMPLATE_DEBUG = DEBUG

ADMINS = (
#('Admin', 'admin@example.com'),
)
MANAGERS = ADMINS

# Enable Compress
COMPRESS_ENABLED = True

# Make this unique, and don't share it with anybody.
#SECRET_KEY = 'change me'

# specific timezone settings
TIME_ZONE = None

# remove fcgi script name from url (lighttpd, nginx)
FORCE_SCRIPT_NAME = ''

# Email Settings Overrides
DEFAULT_FROM_EMAIL = 'dms@example.com'

ALLOWED_HOSTS = [
    "www.mydomain.com"
]
